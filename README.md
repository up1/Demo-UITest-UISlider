# Demo-UITest-UISlider
UI Testing for UISlider

1. Default UISlider
2. UISlider with rotationAngle = -Double.pi
3. UISlider with rotationAngle = Double.pi / 2
4. UISlider with rotationAngle = -Double.pi / 2

